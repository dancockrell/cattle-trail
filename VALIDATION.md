# Local validation evidence — 2026-09-08

Godot 4.3 stable, Windows, OpenGL compatibility renderer on NVIDIA RTX 4070.

- Extraction checks pass 13 families and 912 distinct RGBA cells: binary alpha, nonempty padded bounds, source checksums, valid frames/clips and exact tree-layer reconstruction.
- Selected-art checks pass 326 actor frames, valid clip anchors, optional frame durations, attachment sockets, event ordinals and ground provenance.
- Reusable variable-grid integration reproduced every existing atlas byte-for-byte before adding new two-row sequence sheets.
- Full native room QA passes mounted movement, eight-direction selection, gait-phase retention, planted cast travel-stop and queued movement resumption, action timing, scenery/wagon collision, conversation, shooting, lasso following, all-six objective, cash and narrow control bounds.
- Fresh continuous scripted play completes all six cattle from normal spawn using real travel/actions, with no actor teleports. Saved as continuous-play.mp4.
- Eight-direction cast review asserts visible flight before neck attachment, a single loop/tether and low-hand recovery for all eight facings, including the earlier southwest release. Its walk section includes actual translation over fixed ground. Anatomical phase acceptance remains outstanding.
- Design JSON Schema checks pass three named companion records, reject age17 and accept age18. These are design records, not loaded gameplay systems.
- Current Windows export starts successfully, renders all actor textures, and passes the exported kit browser check for all 13 families and 912 cells. This is a review build; the open animation gate remains.

Current room screenshots are actual engine output. Older room-playback, pose-review and herd-review movies are historical comparison records. Source tests and exports do not imply user visual approval. See VISUAL-AUDIT.md for the concrete remaining animation work. Git history is local; no push or remote publication is claimed.

Latest parallel batch: 16 longhorn state sprites, 16 grass/plant clusters and 8 rider walk candidates extracted; all 816 kit cells pass source/alpha/bounds checks. Full gameplay QA passed with the new grass selection and revised neck attachment. The subsequent Windows export and kit browser verification cover all 912 cells.

Movement production follow-up: 24 additional whole sprites extracted, bringing the current candidate count to 936. movement-batch-review.mp4 renders the east rider and rear-oblique human movement strips at native room scale. stride-v8-comparison.mp4 compares the existing and newer northeast rider sequences. These review modes preserve the current room selection. Windows export/launch and kit browser loading passed for 936 cells.

Speech/reaction integration: native desktop and390px-phone captures pass world-bound checks; movement continues during a displayed comment. The rustler reacts before fleeing after two public shoot actions. Full gameplay QA passes with the greeting, explicit reaction clip and revised phone HUD spacing.

Paced locomotion update: full gameplay QA passed with32px/sec mounted movement, the compact V8 NE walk,48px/sec rustler run and18-second leads. Eight-direction cast review passed with cycle timing derived from the active clip. Tests verify behavior and timing, not anatomical perfection.

The continuous paced demo completed all six cattle through normal travel and actions in80 seconds (4,802 recorded frames at60fps), without actor teleports. Windows export launch and the desktop/phone speech checks passed again with the revised movement pace.
Dedicated character pass: 936-cell kit contracts and the 326-frame room selection pass after integrating the rider's four-pose v9 east walk, shared measured anchor and per-frame hand sockets. The native 16-second moving-direction recording passes all eight facings for rider, longhorn, cream and spotted cattle. Full native gameplay QA passes movement, dialogue, shooting, lasso following, all-six completion and phone bounds. The refreshed Windows export loads 30 scene actors and saves its smoke capture successfully. An initial headless QA attempt was stopped because this harness requires native rendering for captures; only the subsequent completed native run is counted as a pass.
Companion gameplay extension: the first native activity check passes recruitment, separate relationship state, control transfer, three unique cattle actions, return-to-camp completion, capped rest and six phone controls. The extended check walks Eleanor through the activity, restores her active room save, chooses the separate courting beat and checks recovery cooldown. State validation passes 77 checks, including version-1 migration into three-human madness state, invalid-save atomicity and duplicate-reward prevention. The original full herding QA also passed after companion integration.

The 944-cell extraction and 326-frame room selection pass. Native eight-direction playback now includes the grounded northwest rider v10 sequence. The 24-facing neck review exposed and corrected the lead drawing over the cow's back: the attached lead is now occluded by the sprite, while its near neck arc remains in front. Turn-source candidates are not yet admitted.
The refreshed Windows export starts successfully with all 30 scene actors and saves its native smoke capture. The current Eleanor activity movie is 36 seconds and begins at the herd-completion boundary; it is not a recording of the preceding cattle drive. It includes actual Eleanor walking, interactions and camp return. The extended native test also verifies active-room save restoration and the separate courting choice. Desktop retains integer 2x world enlargement at 1280x1000; phone controls use three rows without overlapping the world.
Use `python tools/verify_godot.py state room companion motion` for guarded checks. The wrapper rejects SCRIPT ERROR/ERROR output and missing completion markers even when Godot exits zero, and bounds each owned process to 180 seconds by default. `--self-test` executes a real failing assertion that still prints a completion marker and exits zero; the wrapper correctly rejects it. This guard was prompted by Pirate Island Work's verified finding and reproduced locally. A passing process exit alone is insufficient evidence.
Current production direction: prioritize sheet correction and engine/backend work; do not repeat in-game playback checks while poses are being repaired. Focused headless phase checks passed unequal frame-hold remapping and loop boundaries. Focused storage checks passed staged replacement, backup recovery, corrupt-primary repair and failed-write preservation. These do not certify the new walk drawings. The Windows package still reflects the earlier native-verified checkpoint until a later build is requested or appropriate.

8 September source production follow-up: 948 extracted catalog cells and 330 selected actor frames pass atlas/source/anchor/clip checks. Four northern rider poses now bridge NW↔N and N↔NE. The turn scheduler passes authored-mode, missing-bridge, phase, finite-hold, retargeting, cancellation and completion checks. Storage and room-snapshot checks pass schema fallback, validator isolation, finite coordinates, encounter consistency and JSON roundtrip. All 11 runtime scripts compile without instantiating scenes. Ten Eleanor banter records connect to actual events with saved beat IDs preserved. No gameplay playback or Windows export ran for these changes; the executable remains the older 7b351c0 checkpoint. Working Eleanor turn, rider v16 and rustler v5 sheets are candidates, not accepted runtime walk replacements.

Trail-clock production check: elapsed simulation, paused and invalid deltas, midnight rollover, and daily recovery unlocking after play pass in a headless state-only script. Twelve runtime sources compile without scene instantiation. No gameplay test or new Windows export ran.

Lantern backend: guarded headless suite passes 46 checks covering ordered checkpoints, stable cattle identities, pause/resume, recoverable failure/retry, strict atomic save loading and a single completion milestone. Outer snapshot checks pass old-save compatibility and recruited ownership of started lantern checkpoints. All 13 runtime scripts compile without scene instantiation. This is backend evidence only; no lantern encounter gameplay, carry walking or room visual acceptance is claimed. Eight new whole-character lantern/spirit poses remain source working kits outside the 948-cell main catalog.

Lantern presentation checks pass checkpoint views, pause/retry, immutable simulation state and exact restored presentation. All 15 runtime scripts compile without instantiating scenes. The optional art builder verifies seven exact whole RGBA cells, binary transparency, bounds, anchors, sockets and source hashes. No game run or Windows export was performed.

Room-controller integration check passes in a headless isolated fixture without scene/texture rendering: entry, Talk selection without instant credit, actual follower travel for all three cattle, base-animation ownership, pause/resume and one-time trust reward. Source compilation passes 17 runtime scripts. Outer save validation rejects lantern progress before the first companion activity completes, preventing contradictory simultaneous activities. No rendered gameplay run or Windows export was performed for this integration; art placement and movement remain visually unverified.

Spirit-exposure follow-up: lantern backend now passes 51 checks, including single application, saved deduplication and v1 migration without retroactive stress. The isolated controller check verifies Eleanor rises from10 to16 once while settling the spirit. Snapshot compatibility and 17-source compilation pass. No rendered gameplay or Windows export ran.

Eleanor carry v5: exact-cell builder validates five whole transparent64×64 frames assembled in alternating contact/passing order plus idle. The east equipment presentation uses these whole sprites and phase transfer; other facings keep belt equipment. Source compilation passes17scripts. Ada and steam-handler candidate sheets were inspected statically with direction/continuity caveats recorded. No rendered gameplay or Windows export ran.

Banter queue:24 focused checks pass priority, FIFO, bounded replacement, expiration, deduplication and payload isolation. A separate isolated test calls the actual room delivery methods without adding a room scene or loading textures; deferred history, duplicate suppression, expiry and a freed speaker all pass. All18 runtime sources compile. No rendered gameplay test or Windows export ran.

Steam repair backend passes36 checks for pressure/action ordering, unstable testing, overpressure retry, partitioned time and strict atomic saves. Ada companion checks pass repair-before-invitation, declined invitation without changes, accepted recruitment once, adult identity and atomic save restoration. Outer room saves accept optional Ada state and reject malformed types. All20runtime scripts compile. No game scene was launched and no Windows export ran.
